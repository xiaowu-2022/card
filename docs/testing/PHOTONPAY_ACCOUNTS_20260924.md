# PhotonPay 多账号与 BIN 占用验收（2026-09-24）

本次仅实施代码、隔离 PostgreSQL `card_ui_test` 离线测试、静态前端验收及业务库只读预检。未执行正式/沙箱开卡、充值、激活或历史回调重放，也未迁移当前业务库。

## 结果

- 卡业务及 PhotonPay 回归：309 passed / 2101 assertions。覆盖虚拟/实体开卡、收件人、激活、资金与 UNKNOWN、交易查询、通知、产品和卡商。
- 后续新增后台原子创建与补强回调处理断言后，最终账号/BIN 专项：16 passed / 88 assertions。
- 本地隔离模拟通知 CLI 改为明确的合成 inbox 路径后，独立复验：1 passed / 9 assertions。不替换全局或真实账号公钥。
- TypeScript、ESLint、Vite 构建、Pint、`git diff --check` 通过。Vite 仍有既有 bundle 大小提示。
- 中文后台静态 Inertia/Playwright 验收：编辑密钥输入为空；账号表单环境、身份、检查提示及回调地址；产品选择账号后展示目录；已占用 BIN 显示所属卡商，aria-disabled=true；未占用 BIN 可选；浏览器无运行错误。使用合成展示数据，不提交真实账号配置。

专项覆盖：凭据保留/轮换/版本冲突/配置失效，验证错误不闪存秘密，创建失败整笔回滚，账户归属不符拒绝，缺账号/暂停/非目录 BIN 拒绝，跨账号及正式/沙箱重复拒绝，归档和改 BIN 永久占用，数据库唯一约束兜底，同 BIN 长短串独立，使用后身份锁定，两账号独立 Token 和签名密钥，独立公钥与相同公钥情况下的跨账号资源拒绝，旧地址唯一账号定位及事件去重。

## 只读预检

`php artisan cards:accounts-preflight` 在当前本地业务库输出：

- `.env` 依赖的未绑定产品：0。
- `522105`：同卡商两条已归档历史产品，保留永久占用。
- `53493435`：同卡商一条已归档、一条未归档，保留两条；迁移优先指向未归档产品。
- 多账号/BIN 新迁移尚未在该业务库执行，因此预检返回“迁移后重跑”。这不是已完成部署或线上账号检查的证明。

上线需执行迁移、核对账号与占用、补齐独立公钥并完成只读连接检查、发布前后端及重载 PHP。迁移身份冲突和任何未确认的历史 `.env` 账号不能自动切换。详见架构和发布文档。
