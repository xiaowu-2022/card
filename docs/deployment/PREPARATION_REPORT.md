# 部署准备验收记录

日期：2026-09-15。范围：本地迁移清单、备份恢复、部署文档及只读预检查；未执行正式发布。

## 通过项

- 58 个迁移文件全部保留，生成 SHA-256 清单；当前库已执行 58 项，无待执行项，已执行迁移均有源码。
- 完整离线备份已保存至 `/Users/eaxyqminuo/www/card-backups/pre-production-20260915-132536`。两库、配置、私有 storage 和含未提交文件的源码全部保留。
- 全部归档与 dump 的 SHA-256 校验通过。两份 dump 均在全新临时归档库完成 `pg_restore --single-transaction --exit-on-error`，关键数量与备份快照一致，恢复库账本余额等于已封存分录之和。
- 仅清理恢复脚本自行创建的临时归档库；原 card_mock、card_platform 保留。检查没有残留 `card_archive_check_*` 库。
- 原 `.env` 与备份逐字节一致；5275 个 `storage/app` 文件内容逐一校验一致。归档另有 1 个 macOS 自动生成的 AppleDouble 元数据附属文件，未作为丢失业务文件处理。
- 8000 app 和原退款进程均恢复运行；未重启 8001 服务。
- 只读 inventory 检查成功；`--production` 完成检查并以非零状态正确拒绝当前 Mock/沙箱、本地域名、通用密码及本地运行配置，无异常中断。
- 当前库 `ledger:reconcile` 通过，无余额与分录总计差异。
- 新 PHP 工具通过语法检查和 Pint；两份 shell 脚本通过 `bash -n`；变更文件空白检查通过。
- 配置示例中的实际 PhotonPay 凭据已清空，原值仅保留于受限备份与现用私有配置；私有存储、配置、密钥、备份和缓存从 Docker 构建上下文排除。未改变实际接口密钥或持久数据密钥。

## 恢复数量

| 数据库 | 迁移 | 用户 | 卡片 | 账本事件 | 账本分录 |
| --- | ---: | ---: | ---: | ---: | ---: |
| card_mock | 58 | 502 | 11 | 1286 | 2572 |
| card_platform | 39 | 2 | 1 | 6 | 12 |

本次不修改应用功能，因此未重跑上一轮完整 1073 项 PHP / 67 项前端测试及构建；这些是上一轮端口统一后的结果，不能冒充本次生产验收。当前仅执行与备份、恢复、迁移清单和只读工具相关的检查。

## 仍未通过的正式上线项

- 模拟/沙箱与测试资金来源需要明确生产迁移边界，不能整库直接切真实业务。
- 正式域名、服务器、TLS/反向代理、生产商户、通知与链验证配置尚未验收。
- 域名验证仍是本地白名单实现，需要真实 DNS 验证。
- 本地通用密码与曾进入配置示例的接口凭据不能作为生产凭据。
- 异地加密备份、生产最小权限数据库角色、灾备和监控尚需落地。
- 队列/调度/链扫描只在审查既有订单与起点后启用，本次未启用任何正式后台任务。

完整步骤和故障恢复策略见 [部署手册](PRODUCTION_DEPLOYMENT.md)。
