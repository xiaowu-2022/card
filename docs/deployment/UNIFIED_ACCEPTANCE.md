# 统一部署验收（2026-09-15）

- 用户明确批准：全部现有数据保留在同一部署站点，撤销此前必须分开保留模拟/沙箱数据的部署约定。
- 原 58 个迁移保持不变；新增第 59 项，仅解除商户/持卡人触发器中的数据库名限制，保留身份与归属保护。
- 完整后端回归：1078 项通过，8921 条断言。最后卡片可用性调整后相关回归：126 项通过，1206 条断言。前端检查：67 项通过。
- 两次在独立临时数据库/临时文件目录演练完整恢复，第二次包含并发锁和 PostgreSQL 版本检查。原站点、原数据库、原 `.env` 未被恢复程序覆盖。
- 恢复后迁移 59 项、公司 2、用户 502、卡片 11、账本事件 1286、分录 2572，账本对账通过。
- 恢复后的 10 张 PhotonPay 连接卡、1 张 LOCAL_MOCK 连接卡均仍由原类型处理；加密模拟状态可解密，卡片页可用性正常。验证过程禁止并确认没有外部 HTTP 调用。
- 重复对非空目标执行导入会拒绝；并发恢复锁验证通过。
- 只读预检查区分业务数据存在与部署完成，保留默认密码提示，目录商户记录不再被作为必须删除的数据。
- 恢复过程保持维护状态，不启用队列、调度、历史重放或真实资金操作。
- 尚未连接用户服务器执行部署；真实域名/TLS、通知公钥及后台任务启用仍需在该服务器验收。

- 最终类型检查及前端构建通过。最新离线备份：`/Users/eaxyqminuo/www/card-backups/unified-site-20260915-161938`；本地服务已恢复。
